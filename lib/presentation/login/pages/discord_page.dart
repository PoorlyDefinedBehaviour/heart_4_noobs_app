import 'package:flutter/material.dart';

class DiscordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("DiscordPage"),
    );
  }
}
